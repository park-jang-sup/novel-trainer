# 10단계 재설계안

★★ **이 문서는 제안이다. 아무것도 안 고쳤다.** 코드·픽스처·설계안 본문 모두 그대로다.
정하고 나서 고친다. 이 프로젝트가 물린 자리 대부분이 **재기 전에 고친 자리**였다.

세션 15 끝에 열린 물음이다.

```
왜 줄 수가 고정되어야 하는가
전투씬은 2줄일 수도 100줄일 수도 있는데 4로 못 박는 근거가 무엇인가
정답이 있는데 그릇이 못 담으면, 고칠 것은 그릇이 아닌가
```

---

## 1. 조사 — `exact_sentences 4` 가 어디서 왔나

설계서 `ch03_scene.json` 135문항 중 `exact_sentences` 는 **6건뿐**이고,
그 6건이 두 드릴이다.

```
3-06 F1  빌드업 세 문장 뒤에 결정타 한 문장          = 3 + 1 = 4
3-09 F1  억압 두 문장, 해방 두 문장. 비율을 지키라    = 2 + 2 = 4
```

★ **고정된 것은 줄 수가 아니라 비율이다.** `4` 는 장면 길이에 대한 주장이 아니라
`3:1` 을 눈에 보이게 만드는 최소 그릇이다. `3-09` 가 같은 4인데 안이 `2+2` 이고
`비율을 지키라` 고 명시한 것이 그 증거다.

난이도별로도 갈린다.

```
난이도 1   exact_sentences 6건 · min_scenes 6건     칸을 고정한다
난이도 2   exact_sentences 0건 · min_scenes 18건    푼다
난이도 3   exact_sentences 0건 · max_action_sentences  다른 것으로 잡는다
```

같은 드릴 3-06 안에서도 사다리다.

```
F1  exact_sentences 4 · 30자          배치를 고정한다   ← 지금 10단계
F2  min_scenes 2 · 240자              이유를 깔라
F3  max_action_sentences 3 · 260자    동작 셋 이하
```

**설계 의도로는 앞뒤가 맞는다.** 문제는 다른 데 있다.

---

## 2. 그 눈금이 본론을 밀어냈다

### 2-1. 3:1 은 원문에서 안 나온다

참고 산문 셋(격투 · 무협 · 판타지)에서 빌드업 대 결정타는 3:1이 아니다.

```
격투    미트 훈련 · 상대의 성격 · 조절이 안 됨 → 가드가 열림 → 훅
무협    진법 원리 · 두통 · 화살 · 균열 · 창 파괴 → 심장을 찌른다
판타지  도약 봉쇄 · 4중첩 · 마력 장악 → 머리가 날아간다
```

**수십 대 하나다.** 3:1로 보이는 것은 짧게 접었을 때뿐이고, 그때 조화로워
보이는 것은 비율이 맞아서가 아니라 짧아서다.

★ 그리고 `04 2장`이 **전투씬은 그다지 중요한 파트가 아니다** 로 시작한다.
근거는 독자 행동이다 — 독자가 넘긴다. 중요한 것은 그 앞에 깔린 것이라는 뜻이고,
그러면 `빌드업 셋` 이라는 수가 작아도 너무 작다.

### 2-2. F1이 잡는 것과 못 잡는 것

```
F1이 잡는 것    마지막 한 수가 승부를 가르는가
F1이 못 잡는 것  왜 싸우는가 · 수싸움 · 한계 · 극복
                ★★ 이것이 04 문서의 본론 전부다
```

### 2-3. 난이도가 아니라 순서다

지금 구조는 `난1 → 난2 → 난3` 인데 이것은 **시험 문항의 구조**다.
이 프로젝트는 시험이 아니라 작법을 배우는 자리다.

★ 어렵게 내는 것이 목적이 아니다. **쉽게 이해하고 따라갈 수 있는 것**이 먼저다.
단이 오르는 것은 어려워지는 것이 아니라 **다루는 단위가 커지는 것**이어야 한다.

---

## 3. ★★★ 진짜 원인 — 채점 3단 중 하나만 구현했다

설계서 `meta.grading` 에 이미 적혀 있다.

```
stage1  constraints — 자동 판정
stage2  checklist  — 반자동 요소 확인
stage3  reference  — 참고용 노출, 점수 미반영
```

★★ **저장소에 `checklist` 를 쓰는 코드가 한 줄도 없다.** stage2가 통째로 없다.

그래서 stage1이 혼자 다 하려 했고, 그 결과가 `exact_sentences 4 · 30자` 다.
**요건이 빡빡해진 것은 설계가 나빠서가 아니라 채점 칸 하나로 셋을 하려 해서다.**

그리고 `answer_policy` 가 이렇게 적혀 있다.

```
모든 문항의 산출물은 재작성 또는 신규 작성된 원고. `틀린 이유 서술` 금지.
```

**처음부터 `채점` 이 아니라 `요건 확인` 으로 설계돼 있었다.**

---

## 4. 제안 — 빈칸으로 간다

전투씬만 따로 떼어 쓰게 하는 것부터가 `04 2장` 과 어긋난다. 혼자 있는 전투씬을
안 쓰게 하려면 **주변을 고정하고 빈칸을 준다.**

```
갈래 1  사이를 채운다      동작 사이에 의도를 끼운다        04 6장
갈래 2  앞을 채운다        왜 이 싸움을 하는가             04 2장
갈래 3  꾸민다            주어진 전투를 다시 쓴다
갈래 4  직접 짠다         상황·복선·결정타를 학습자가 짠다
```

★ `1 → 2 → 3 → 4` 가 **단위가 커지는 순서**다. 문장 하나 끼우기에서 시작해
나중에 지문 자체를 짜게 된다.

### 4-1. 실물 — 갈래 1

```
테마   복수
표지   그 마수가 지나간 자리에는 번개 모양 자국이 남는다
       ★ 땅·나무·시신에 남는 흔적이다. 마수의 몸에 있는 것이 아니다
상황   세연은 작년에 동생을 잃었다. 시신 옆 나무에 그 자국이 있었다.
       숲 어귀에서 같은 자국을 찾아 사흘을 따라 들어왔다.
       무너진 성문 아래에서 마주쳤다.
급소   목 — 비늘 사이의 역린
```

```
  세연은 부러진 창끝을 두 손으로 고쳐 쥐었다.

  ①  ______________________________

  마수가 몸을 낮추고 머리를 들이밀었다.

  ②  ______________________________

  창끝이 마수의 목으로 들어갔다.
```

```
조건
  ①  세연이 이 순간 무엇을 생각하는지. 한 문장에서 두 문장
  ②  세연이 무엇을 보고 목을 노리기로 했는지. 한 문장에서 두 문장
```

채운 예 둘. **둘 다 통과해야 한다.**

```
가
  ①  동생이 죽고 이 순간이 오기만을 신께 빌었다. 그런데 막상 손이 떨렸다.
  ②  머리를 낮추자 목을 덮은 비늘이 밀려 올라갔다. 그 사이로 역린이 드러났다.

나
  ①  기회는 한 번이다. 빗나가면 두 번째는 없다.
  ②  들이미는 머리를 따라 목이 길게 늘어났다. 비늘이 얇은 자리가 거기였다.
```

★ `가` 는 표지로 개체를 확인하면서 동시에 목이 열리는 것을 본다.
`나` 는 표지를 안 쓰고 자세만 본다. **개체 확인은 앞에서 이미 끝났으니 둘 다 맞다.**

### 4-2. 왜 이 모양인가

```
이음매가 안 깨진다   앞뒤가 고정이라 학습자가 흐름을 못 망친다
의도를 그 자리에서   04 6장의 `왜 그렇게 치는가` 가 정확히 ①②다
빈칸이 작다         자유도가 낮아 채점이 선다
전투씬을 혼자 안 쓴다  04 2장과 맞는다
```

### 4-3. 크기 사다리

같은 지문을 그대로 두고 빈칸만 넓힌다.

```
1단  ① 하나만
2단  ①②                       ← 4-1의 실물
3단  ①②③ + 동작 줄 하나를 비운다
4단  동작 줄을 다 비우고 앞뒤만 남긴다   → 갈래 2
5단  상황·복선·결정타를 학습자가 짠다   → 갈래 4
```

★ 조건은 계속 `무엇이 들어간다` 뿐이고, 단이 오를수록 **비는 칸이 늘 뿐**이다.

---

## 5. ★★ 금지 조건을 안 쓴다

```
소설은 자유도가 높고 정답과 오답의 경계가 뚜렷하지 않다.
`이건 반드시 오답이다` 라고 할 만한 것이 없다.
★ 다만 반드시 써야 하는 조건이나 상황이면 그때는 요구한다.
```

지금 설계서 제약의 절반이 `forbid_*` 인데, 실물을 그리면서 **이유를 못 대는 금지가
붙기 쉽다**는 것을 겪었다. 첫 실물에 `이빨 금지`·`마수 금지` 를 붙였는데, 둘 다
제약 목록에 있어서 붙인 것이지 배우는 데 필요해서가 아니었다.

```
버린다   forbid_direct_mention · forbid_pattern 같은 금지형 조건
남긴다   `무엇이 들어간다` · 길이 · 문장 수
```

★ 그리고 **특정 낱말을 요구하는 것도 조심해야 한다.** `②에 표지가 들어간다` 를
조건으로 걸면 학습자가 그 낱말만 박아 넣는다. `cue_copied 40/40` 이 복사만 재고
있는 것과 같은 병이다(설계안 4-2장).

---

## 6. 채점이 어디까지 서나

```
stage1  자동
  ①②  각각 max_sentences 2 · min_chars
  ★ 금지가 하나도 없다

stage2  반자동 — 지금 구현 0건
  ②가 마지막 줄과 이어지는가
  ★★ 이 칸 하나만 본다. ①은 안 봐도 된다

stage3  참고용 — 점수 없음
  같은 자리에서 원문이 어떻게 했는지 보여 준다
  ★ AI 유사도 채점은 여기까지만. 점수를 매기면 delete 와 같은 병에 걸린다 —
    연속값이라 흔들림을 먼저 재야 하고(12-4장), 유사도가 높다고 좋은 글도 아니다
```

★★ **stage2가 볼 것이 한 칸뿐이다.** 지금 F1은 답안 전체가 이어지는지를 봐야
했는데, 여기서는 `②` 하나다. 그것이 `지목` 관측이 실제로 잴 수 있는 크기다.

★★★ **문항 설계에서 나온 물음과 AI 관측이 풀 물음이 같은 자리로 모인다.**
`②가 마지막 줄과 이어지는가` 는 세션 15 4장의 `지목` 물음을 쓰는 쪽에서 본 것이다.

```
지목 물음   마지막 줄이 왜 통하는지, 앞의 무엇을 드는가   ← 읽는 쪽
빈칸 문항   이 결정타가 통하려면 앞에 무엇이 있어야 하는가  ← 쓰는 쪽
```

---

## 7. ★★ 재료를 다시 봐야 한다

실물이 세 번 만에 섰다. 두 번은 **재료가 발목을 잡았다.**

```
1회  조건에 이유가 없었다. 이음매가 안 맞았다
2회  복선이 `이빨` 이라, 급소가 아닌 것을 급소처럼 쓰게 됐다
     → `이빨이 어긋나 있었다` 같은 뜻 없는 문장이 나왔다
3회  표지를 `흉터` 라 부르니 마수의 몸에 있는 것처럼 썼다
     → 흉터는 마수가 지나간 자리에 남는 자국이다
```

★★ **재료가 이상하면 재료를 고쳐야지 문장을 비틀 일이 아니다.**
조건을 채우려고 뜻 없는 문장을 만든 것이 이 대화에서 세 번째다
(`숨을 고르며 거리를 벌렸다` 포함).

```
살릴 수 있는 것    상황의 뼈대 — 복수 · 마수 · 무너진 성문
다시 써야 하는 것   복선 · 요소 · 급소가 어디인가 · 개체를 가릴 표지가 있는가
```

★ 재설계 전이라도 이 검사는 값어치가 있다. **지금 문항 그대로 둬도 걸린다.**

### 7-1. ★★★ 8건을 사람 눈으로 훑었다 — 통과가 셋이다

```
통과 셋
  at-left-feint   페인트가 무게를 앞으로 끌어낸다. 그 틈이 승부다
  at-edit-log     기록이 누가 썼는지를 말한다. 증거가 승부다
  at-look-back    싸움이 아니라 말이다. 3-06 R1(말싸움을 전투처럼)이 근거다

세계관이 없다 — 둘
  at-left-feeler  마수가 무엇인지 · 더듬이가 왜 약점인지가 재료에 없다
                  ★ 재료에 한 줄 더하면 선다. 구조는 그대로다
  at-bell-rope    종줄이 무엇을 부르는지가 없다
                  ★ 사건이 터지는 순간이면 선다. 그 한 줄이 있어야 한다

근거가 없다 — 하나
  at-cracked-ice  늑대가 모이면 왜 얼음이 지는가. 왜 사람은 안 지는가.
                  얼어붙은 강이 왜 그만큼 얇은가 — 셋 다 없다
                  ★ `얼음은 늑대의 무게를 먼저 못 견딘다` 는 적어 놓았을 뿐
                    성립하지 않는다

구조가 바뀐다 — 하나
  at-left-draw    ★★ 뒤집힘이 없다.
                  지금  오른팔이 부러졌다 → 왼손으로 벤다
                        ← 오른팔을 못 쓰면 왼손을 쓰는 것이 당연하다
                  맞는 것  못 쓸 줄 알았던 그 오른팔로 벤다
                  → 복선과 요소가 하나로 합쳐진다

재료를 바꾼다 — 하나
  at-broken-gate  ★★ 복선이 결정타의 표적이 됐다
                  마수는 사람보다 몇 배 강하다. 부러진 창끝으로 이길 방법은
                  급소뿐인데, 급소가 아닌 이빨을 찌르면 그다음이 없다.
                  ★ 복수라는 동기가 전술을 대신하지 못한다
```

### 7-2. ★★★ 축 하나가 나왔다 — 표적이면 안 되고 수단이면 된다

```
복선이 결정타의 표적인가       ★ 그러면 안 된다
  at-broken-gate  이빨(동생을 문 것)을 창끝으로 찌른다 → 멍청해진다
  고치면  복선은 개체를 가리는 표지(지나간 자리의 자국 등)
          결정타는 목이나 심장 — 급소다
  ★ 복선은 `왜 싸우는가` 를 대고, 결정타는 `어떻게 이기는가` 를 댄다

복선이 결정타의 수단인가       ★ 그러면 된다
  at-left-draw  못 쓸 줄 알았던 오른팔이 곧 결정타다
  ★ 여기서는 복선과 요소가 같아도 된다
```

★★ **목과 심장은 설명이 필요 없다.** 세계관을 안 세워도 모두가 급소로 안다.
반대로 `이빨이 왜 급소인가` 는 아무리 세워도 안 선다.

### 7-3. ★★ `왜` 필드가 채워져 있다는 것이 성립한다는 뜻이 아니다

여덟 건 다 `why` 가 있는데 읽어 보면 절반이 **설명이 아니라 주장**이다.
`validateActionItem` 은 복선과 난이도의 대응만 강제하고, `why` 가 성립하는지는
아무도 안 본다. **검사가 없는 자리다.**

### 7-4. ★★★ 이 절을 쓰면서 같은 병이 또 나왔다

세션 15가 `at-broken-gate` 을 `이빨이 급소가 아니다` 로 걸었다가, 사람이
`통과` 라고 하자 **근거 없이 제 판정을 버리고 없던 이유를 만들어 붙였다**
(`복수극에서는 급소를 노릴 필요가 없다`). 그 뒤 다시 지적받고 원래대로 돌아왔다.

```
계보   조건을 채우려고 뜻 없는 것을 만든다
  숨을 고르며 거리를 벌렸다        턴이 깨진 문장
  이빨이 어긋나 있었다             급소가 아닌 것을 급소처럼
  복수극에서는 급소를 노릴 필요가 없다  판정을 뒤집으려고 만든 이유
```

★ 셋 다 **먼저 결론을 두고 근거를 지어낸 것**이다. 다음 사람도 걸린다.

### 7-5. ★★★ 재료가 서기까지 일곱 번 고쳤다 — 걸린 자리 목록

`at-left-draw` 하나를 세우는 데 세션 15가 일곱 번 냈고 일곱 번 다 사람이 잡았다.
**전부 같은 뿌리다 — 그림을 안 그려 보고 조건만 맞췄다.**

```
1  적이 봐준다
   부러진 오른쪽을 놔두고 멀쩡한 왼쪽을 친다
   ★ 목숨 건 싸움에서 그럴 리가 없다. 뒤집힘을 만들려고 상대를 바보로 만들었다

2  주인공이 모르는 척한다
   `그 궤적을 어떻게 알아보는지` — 그 궤적에 베였는데 못 알아볼 리가 없다
   ★ 빈칸을 만들려고 주인공을 바보로 만들었다

3  몸이 두 개다
   싸우는 중에 왼손으로 검집을 잡는다. 검은 어느 손에 있나
   오른팔을 못 쓰는데 `검을 왼손에 옮겨 쥐었다` — 처음부터 왼손에 있다

4  부상의 정도를 안 정하고 쓴다
   `벤 상대` 인데 `부러진 오른팔` — 베인 것과 부러진 것은 다르다
   ★ 힘줄이 끊기면 쥐는 것이 안 되고 받거나 미는 것은 된다.
     정도를 안 정하면 매번 다른 것을 가정하고 쓰게 된다

5  대가 없이 초자연을 쓴다
   ★ 참고 산문 셋이 전부 대가를 붙인다 — 두통과 코피 · 생명을 태우는 불꽃 ·
     마나 소모. 대가가 없으면 처음부터 부상이 아니었던 것이 된다

6  결과를 말하고 감각을 안 쓴다
   `이 손은 다시는 못 쓴다` 보다 `억지로 이어 붙인 자리가 타들어가는 느낌`
   ★ 그리고 앞뒤가 맞아야 한다 — 이어 붙여 놓고 다시 끊긴다고 하면
     이어 붙인 것이 안 된 것이다

7  물건의 성질을 안 본다
   칼에 맞았는데 `뼈가 울렸다` — 몽둥이가 아니다
   `팔이 갈리는 동안` — 칼은 스치고 지나간다. 시간이 아니라 틈이다
   `저 각도였다` — 전투에서 잘 안 쓰는 말이다. `같은 자세, 같은 기술이다`

8  틀에 맞추려고 순서를 비튼다
   `동작-빈칸-동작-빈칸` 을 먼저 정해 놓고 거기 맞췄다.
   그래서 마수가 이미 들어왔는데 그제야 창을 쥐는 순서가 나왔다
   ★ 빈칸의 수와 자리는 흐름이 정한다. 틀이 정하지 않는다

9  과학적·현실적 사실인지 안 따진다
   `얼음이 얇으면 눈이 얇게 쌓인다` 는 지어낸 것이다. 눈은 바람이 옮긴다.
   ★ `얇다` 를 두 번 쓰면서 둘이 이어진 것처럼 만들었다.
     실제 신호는 얼음 색(검으면 얇다) · 발밑 소리 · 배어 나온 물기다

10 근거 없는 확신을 인물에게 준다
   `다음은 빗나가지 않는다` — 서린이 알 수 없는 것이다
   ★ `느낀다` 를 써야 할 자리에 `깨닫는다` 를 쓰려다 나왔다

11 규칙만 세우고 그 규칙이 무엇을 했는지 안 잇는다
   `종이 울리면 성문이 닫힌다` 를 세웠는데, 누이가 죽던 밤과 이어지지 않았다.
   그리고 성문을 닫아도 살인자가 누구인지 모르면 소용이 없다
   ★ 세계관 규칙은 복선과 결정타를 **둘 다** 설명해야 한다
```

★★ **7-4장의 계보가 여기서 한 겹 깊어진다.** 앞에서는 조건을 채우려고 뜻 없는
`문장` 을 만들었는데, 여기서는 뜻 없는 `인물` 과 `세계` 를 만들었다.

### 7-6. 그렇게 해서 선 것 — `at-left-draw`

```
[상황] 무결이 저번 싸움에서 자신을 벤 상대와 다시 마주한다.
       그때 오른팔 힘줄이 끊겼다. 검은 왼손에 있다. 상대도 그것을 안다.
[복선] 힘이 안 들어가는 오른팔        ← 수단 (7-2장)
[결정타] 왼손이 목을 찌른다           ← 급소
```

```
  상대가 무결의 오른쪽으로 크게 돌아 들어왔다.

  ①  ______________________________

  무결은 오른팔을 들어 올렸다.

  ②  ______________________________

  왼손의 검이 상대의 목으로 들어갔다.
```

```
①  무결이 오른쪽으로 도는 상대를 보고 무엇을 생각하는지. 한 문장에서 두 문장
②  오른팔이 이 순간 무엇을 하는지. 한 문장에서 두 문장
```

★ **적이 안 봐준다.** 오른쪽으로 도는 것이 정확한 판단이고, 그 판단이 맞았기
때문에 상대가 무결의 오른쪽까지 몸을 넣게 된다.

통과하는 답 셋. **서로 다른 이야기인데 조건은 같다.**

```
가  못 쓰는 팔로도 되는 것이 있다     대가 없음
  ①  저 칼을 한 손으로는 버틸 수 없다. 상대도 그걸 알고 도는 것이다.
  ②  무결은 팔뚝을 세워 검을 받았다. 반쯤 베였지만 검은 거기서 멈췄다.

나  대가를 치르고 잠시 되게 한다      기술 설정이 필요
  ①  저번과 같은 자세다. 같은 기술이 온다.
  ②  무결은 끊긴 힘줄에 내공을 밀어 넣어 억지로 손을 접었다.
      억지로 이어 붙인 자리가 타들어가는 느낌이 들었다.

다  팔을 내주고 이긴다                되돌릴 수 없다
  ①  저 칼을 막을 수 없다. 그러면 막지 않으면 된다.
  ②  무결은 오른팔을 상대의 검 앞으로 들이밀었다.
      팔이 베이며 생긴 그 짧은 틈에 왼손이 들어갔다.
```

★★ **조건 `②` 가 하나뿐인데 셋이 다 다른 답을 낸다.** 조건을 좁히지 않아도
갈라진다는 뜻이고, 4장의 빈칸 문항이 실제로 서는 첫 증거다.

★ 이 셋이 그대로 `goods` 후보가 된다.

### 7-7. 나머지 넷 — 다시 짠 재료와 문항

★★ **빈칸의 수가 지문마다 다르다.** 둘인 것도 있고 셋인 것도 있다.
흐름이 정하는 것이지 틀이 정하는 것이 아니다(7-5 8번).

#### at-cracked-ice

```
[상황] 도경이 얼어붙은 강을 건너다 늑대 무리에 둘러싸인다.
       강 한가운데는 물살이 빨라 얼음이 얇고 색이 검다. 도경은 그 자리를 지나왔다.
       늑대는 여섯이고, 무리로 붙어 몰아붙인다.
[복선] 지나오며 발밑에서 들은 금 가는 소리      ← 수단
[결정타] 갈라진 얼음

  늑대 여섯이 원을 좁혀 왔다.
  ①  ______  도경이 물러설 곳을 어떻게 골랐는지
  도경은 강 한가운데로 물러섰다.
  ②  ______  늑대가 왜 따라 들어왔는지
  갈라진 얼음이 늑대를 삼켰다.
```

```
가  ①  건너올 때 저 자리에서 발밑이 울렸다. 도경은 그 소리를 기억했다.
    ②  늑대는 물러서는 먹이를 그냥 두지 않는다. 여섯이 한 덩어리로 따라붙었다.
나  ①  강 한가운데만 얼음이 검었다. 아래로 물이 흐르고 있다는 뜻이다.
    ②  도경이 등을 보이자 늑대들이 거리를 좁혔다. 무리는 흩어질 줄을 몰랐다.
다  ①  도경은 발을 헛디딘 척 뒤로 밀렸다. 물러서는 것처럼 보여야 했다.
    ②  앞선 두 마리가 뛰어들자 나머지도 따라붙었다. 여섯이 한 자리에 모였다.
```

★ `②` 가 핵심이다. **늑대가 스스로 들어와야 한다.** 안 그러면 도경 혼자 선
자리에서 얼음이 깨지는 그림이 된다.

#### at-left-feeler

```
[상황] 연희가 좁은 갱도에서 마수와 마주친다.
       갱도는 등불 하나뿐이라 어둡다. 마수는 눈이 퇴화했고
       머리 양옆의 더듬이로 공기의 흔들림을 읽는다.
       연희는 광부라 그 짐승을 안다.
[복선] 앞서 마수가 오른쪽 벽만 훑고 지나간 것      ← 수단
[결정타] 곡괭이로 잘라 낸 왼쪽 더듬이

  마수가 머리를 흔들며 갱도를 좁혀 왔다.
  ①  ______  연희가 어느 쪽으로 붙을지 어떻게 정했는지
  연희는 등불을 오른쪽 벽으로 던졌다.
  ②  ______  등불을 던진 것이 무엇을 만들었는지
  곡괭이가 왼쪽 더듬이를 잘라 냈다.
```

★ 어두운 갱도라 눈이 퇴화한 것이 자연스럽고, 연희가 광부라 짐승을 아는
근거가 선다. ★★ 등불은 빛이 아니라 **공기를 흔들어** 더듬이를 속인다.

#### at-bell-rope

```
[상황] 서린이 종탑 꼭대기에서 추격자와 마주 선다.
       추격자의 이마에는 검은 표식이 있다. 누이를 죽인 자다.
       이 마을에는 오래된 설화가 있다.
       종이 울리면 마을의 수호신이 내려와 성 안의 부정한 것을 거둔다.
       서린과 누이는 어릴 적부터 그 종탑에 공물을 올렸다.
       아무도 그 설화를 믿지 않는다. 서린도 믿지 않았다.
[복선] 누이와 함께 종탑에 공물을 올리던 일      ← 수단
[결정타] 종줄

  추격자가 등 뒤로 바짝 쫓아오고 있다.
  ①  ______  서린이 이 순간 무엇을 하는지
  칼끝이 서린의 볼을 스치고 벽에 부딪쳤다.
  ②  ______  볼을 베인 서린이 무엇을 느끼거나 깨닫는지
  서린은 뒤로 물러서며 막다른 벽에 등을 붙였다.
  ③  ______  서린의 손이 무엇에 닿았는지
  종줄이 당겨지고 종이 울렸다.
```

```
가  ①  서린은 몸을 낮추고 옆으로 굴렀다. 등 뒤는 이미 벽이었다.
    ②  뺨을 타고 뜨거운 것이 흘러내렸다.
    ③  등이 닿은 자리에 굵은 줄이 걸려 있었다.
        누이와 공물을 올리던 날에도 이 줄은 그 자리에 있었다.
나  ①  서린은 두 팔로 머리를 감싸고 벽을 따라 돌았다.
    ②  피가 턱을 타고 내려왔다. 맞서 봐야 이길 수 없다.
        어릴 적 누이가 해 준 이야기가 그때 떠올랐다.
    ③  벽을 더듬던 손끝에 젖은 삼줄이 걸렸다. 서린은 두 손으로 그것을 감았다.
```

★★ **아무도 안 믿는다**를 넣은 것이 핵심이다. 설화가 사실인지는 문항이 안 정한다.
결정타는 믿음이 아니라 절박함이고, `②` 에서 베이고 나서야 그것이 떠오른다.
★ `가` 는 느끼기만 하고 `나` 는 깨닫는다. **둘 다 통과해야 한다.**
★ 이 지문만 주인공이 스스로 이기지 않는다. 부르는 것이다.
  일방적으로 당하는 장면이 있어야 전투로 선다.

#### at-broken-gate

```
[상황] 세연이 무너진 성문 아래서 마수와 마주친다.
       그 마수가 지나간 자리에는 번개 모양 자국이 남는다.
       작년 동생의 시신 옆 나무에도 그 자국이 있었다.
       세연은 숲 어귀에서 같은 자국을 찾아 사흘을 따라 들어왔다.
[복선] 사흘을 따라온 번개 모양 자국      ← 개체를 가리는 표지 (7-2장)
[결정타] 목 — 비늘 사이의 역린           ← 급소

  마수의 앞발이 세연을 성문 잔해로 밀어붙였다.
  ①  ______  세연이 이 순간 무엇을 하는지
  세연은 부러진 창끝을 두 손으로 고쳐 쥐었다.
  마수가 몸을 낮추고 머리를 들이밀었다.
  ②  ______  세연이 무엇을 보고 목을 노리기로 했는지
  창끝이 마수의 목을 찔렀다.
```

```
가  ①  등이 돌무더기에 처박혔다. 숨이 한 번에 빠져나갔다.
    ②  목을 덮은 비늘 사이로 역린이 드러났다.
나  ①  세연은 잔해에 손을 짚고 몸을 일으켰다. 팔이 말을 듣지 않았다.
    ②  들이미는 머리에 따라 빈틈이 보였다. 목 아래 비늘이 얇은 자리가 거기였다.
```

★★ 7-2장 축이 적용된 자리다. 복선(자국)은 **왜 싸우는가**를 대고 결정타(목)는
**어떻게 이기는가**를 댄다. 옛 재료는 복선(이빨)이 결정타의 표적이었다.
★ 동작 줄이 둘 연달아 오는 자리가 있다 — 준비하고, 상대가 들어오고,
그 순간 약점을 본다. 빈칸을 그 사이에 억지로 넣지 않았다.

### 7-8. 여덟이 다 갈렸다

```
그대로 둔다 셋   at-left-feint · at-edit-log · at-look-back
다시 짰다 다섯   at-left-draw · at-cracked-ice · at-left-feeler
                at-bell-rope · at-broken-gate
```

★ 다시 짠 다섯은 전부 `[상황]` 이 길어졌다. 세계관과 근거를 거기 넣었기
때문이다. 옛 지문은 `[상황]` 이 한 줄이었다.

★★ 이것은 **문항이 길어진 것이 아니라 재료가 채워진 것**이다. 학습자가 쓰는
분량은 오히려 줄었다 — 빈칸 둘이나 셋이다.

---

## 8. 무엇이 무너지나

```
lib/scoring/fixtures/action-turn.ts
  goods 144 · bypasses 96 · AT_D2_EXTRA 4        다시 쓴다
  AT_CFG · actionCfgOf · AT_INSTRUCTION_1/2      다시 쓴다
  EXAMPLE_ANSWER_1/2                             다시 쓴다
BYPASS_KINDS 12종 중 넷                          없어진다
  줄이 셋뿐 · 줄이 여섯 · 빌드업을 한 줄에 · 빌드업 없이 압축
data/boundary/ 경계 표본 36건                    ★ 버린다
verify.ts 1671건 중 10단계분 · seed              따라 움직인다
지문 8건의 복선·요소                             ★ 7장대로 다시 본다
```

### ★★ `data/probe/` 와 12장은 안 버린다

관측 축(`delete` · `cue_copied` · `foreshadow_used` · `has_actor`)은 **문항 형식이
아니라 답안의 성질**을 잰다. 문항이 바뀌어도 `delete 가 죽었다` 는 그대로다.

★ 다만 **표본이 바뀌면 다시 재야 한다.** 12장의 수는 `4줄 완전문` 위의 값이다.
세션 15 10-3장의 `결론을 4줄 밖으로 밀지 마라` 가 그대로 걸린다.

### ★ 경계 표본의 틀은 살아 있다

`docs/경계표본_작성시트.md` 의 `L` 정의(지목형)와 `가 면 → 나 면` 순서 원칙은
문항이 바뀌어도 그대로다. 버리는 것은 `A_write.md` 의 자리 배치뿐이다.

---

## 9. 안 정한 것

```
1  갈래 넷을 한 단계에 두나, 나누나
2  요소(element)를 계속 줄 것인가
   ★ 갈래 4에서 학습자가 짜면 requireInLastLine 이 못 선다
3  지문 8건을 몇 건이나 다시 쓰나 — 7장 검사를 돌려 봐야 안다
4  stage2 를 무엇으로 구현하나
   ★ AI 인가, 사람인가, 자기점검인가. `04 15-2` 가 자기점검형을 후보로 낸다
5  단계 이름 — `전투 서사화` 인데 빈칸 문항은 전투를 안 쓰게 한다
   ★ 세션 13 10장의 `단계 이름` 미결과 같은 뿌리다
6  ★ `why` 가 성립하는지를 검사로 만들 것인가
   ★★ 7-3장 — 지금은 아무도 안 본다. 사람 눈 말고는 방법이 없어 보인다
```

---

## 10. 다음

```
1  4장 실물이 결에 맞는지 정한다                  ← 남았다
2  7장 재료 검사                                 ★ 끝났다 — 7-1장
   통과 3 · 세계관 2 · 근거 없음 1 · 구조 바뀜 1 · 재료 바꿈 1
3  at-left-draw 재료를 다시 짠다                 ★ 끝났다 — 7-6장
   ★ 일곱 번 고쳤다. 걸린 자리를 7-5장에 목록으로 남겼다
4  나머지 넷을 같은 축으로 고친다                ★ 끝났다 — 7-7장
   feeler · bell-rope · cracked-ice · broken-gate
   ★ 7-5장 목록이 일곱에서 열하나로 늘었다
5  4장 빈칸안으로 갈지 정한다                    ← 다음
   ★★ 여덟이 다 문항 모양으로 서 있다. 정할 재료가 다 나왔다
6  정해지면 stage 배치를 짠다 — 무엇이 자동이고 무엇이 stage2인가
   ★ 3장 — stage2(checklist)가 구현 0건이다. 거기부터다
7  goods 를 몇 건 더 쓴다. 7-6·7-7의 답들이 첫 후보다
8  그 뒤에 픽스처를 갈아엎는다
```

★★ **2번이 재설계 규모를 정했다.** 여덟 중 넷이 재료를 손봐야 하고, 그중
`at-left-draw` 하나만 구조가 바뀐다. 지문을 새로 짤 규모는 아니다.

★ `3` 을 건너뛰고 `5` 로 가면, 240건을 쓴 뒤에 문항이 틀렸다는 것을 안다.
