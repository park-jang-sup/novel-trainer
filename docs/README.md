# docs

**`STATUS.md` 를 읽어라. 그것만.**

`archive/` 는 경위다 — 인수인계 세션 3~16 · AI심사_설계안 · 경계표본_작성시트.
필요한 문장은 STATUS 로 끌어온다. 저쪽을 고치지 않는다.

`10단계_재설계안.md` 와 `10단계_지문요건.md` 는 다음 일(fill 문항)의 재료라 여기 남는다.

`ai/AI채점_도입_지시서.md` 는 박 님이 준 방향 문서 원안이다(세션 40) — 실행
문서가 아니다. 실제로 무엇을 했는지는 STATUS.md 세션 40 "끝난 것"을 봐라.

`characters.md` 는 인물의 단일 출처다(세션 32 후기). 문항이 인물을 쓰면 여기서
꺼내고, 새 면모를 만들면 여기에도 적는다 — 왕복 규칙. verify 가 활성 문항의
인물 이름이 원장에 있는지 문다.

세션마다 새 인수인계를 쓰지 않는다. STATUS 를 덮어쓴다.
